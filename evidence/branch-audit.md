# Branch audit

The canonical branch record is [`../BRANCH_AUDIT.md`](../BRANCH_AUDIT.md).

The source repository had one public branch, `main`, and no experiment,
release, or `orx/*` branches. The normalized public surface retains one
`main` branch. The identity rewrite changes only reachable Git metadata; the
Claim 1 numeric evidence and its declared limitations remain unchanged.
